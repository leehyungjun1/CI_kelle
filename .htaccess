<IfModule mod_rewrite.c>
    RewriteEngine On
    RewriteBase /

    # public 폴더로 리라이트
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ /public/$1 [L]
</IfModule>

# 보안
<FilesMatch "\.(env|log|ini|md|sql|sh|bak|gitignore)$">
    Order allow,deny
    Deny from all
</FilesMatch>